# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_USER_ID=your_numeric_telegram_user_id

# Anthropic API Key (NOT NEEDED - we use CLI OAuth)
# This is a placeholder. The bot uses `claude` CLI which has its own authentication.
ANTHROPIC_API_KEY=placeholder

# Workspace Configuration
# IMPORTANT: Set this to the aiden-platform repo so Claude can operate on the
# actual codebase (not just the maslow repo sandbox).
WORKSPACE_PATH=/Users/mazlow/aiden-platform

# Firebase / CommandWatcher (remote control channel from dashboard Maslow)
# Download the service account JSON from Firebase Console → Project Settings → Service Accounts
# This enables the CommandWatcher that lets you restart services from the dashboard or Telegram.
FIREBASE_SERVICE_ACCOUNT_PATH=/Users/mazlow/.config/maslow-firebase-service-account.json

# Optional: Soul File for Persistent Identity
SOUL_PATH=~/.claude-mem/soul.md

# Claude-Mem Integration (Enabled - Session-based API)
CLAUDE_MEM_URL=http://localhost:37777
# Optional — if set, sent as Bearer token to Claude-Mem API
CLAUDE_MEM_TOKEN=

# Voice Services (local STT/TTS)
WHISPER_URL=http://localhost:8080
CHATTERBOX_URL=http://localhost:4123
VOICE_NAME=Maslow

# App Server (HTTP/WS API for web and mobile apps)
APP_SERVER_PORT=3117
APP_SERVER_TOKEN=
